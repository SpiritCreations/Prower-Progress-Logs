12-10-2025 - Built infra-01 VM


## Goal
Create and have a stable control VM for automation before workloads.


## Initial State
Fresh proxmox host.


## Changes
- Created VM running Ubuntu server 24.04.3
- Installed Docker, Git, SSH
- installed and configured SSH keys
- Edited config files
/etc/ssh/sshd_config
/etc/ssh/sshd_config.d/50-cloud-init.conf
* edit lines to reflect:
PubkeyAuthentication yes (may need to manually write this into config depending on version)
PasswordAuthentication no
 UsePAM no
- Added weekly maintenance restart timer to clean out junk, also ensures the stack recovers cleanly
## Problems with SSH configs
- Issues enforcing key-only login due to cloud-init.conf file overriding sshd_config file policies.
fixed by changing policy in both configs


- 12/13/2025
- Installed unattended upgrades
- Enabled daily security updates
- Disabled automatic reboot
- Weekly reboot handled by systemd timer 
- Verified through dryrun

- 12/16/2025
- Installed UFW
- Default denied inbound, allow outbound
- Allow SSH rule 
- Firewall enabled and verified through SSH
----
- Installed and configured fail2ban
initial service startus failed because of an invalid jail.local config, containing parameters outside of INI
section headers.
- Resolved by recreating jail.local file using a heredoc to avoid formatting issues.
- Enabled SSH jail only
- Backend (systemd)
- 5 failed auth attempts within 10 minutes trigger a ban
- Ban duration set to 1 hour
- LAN IP ranges whitelisted to prevent admin lockout
#
- verified fail2ban service starts successfully
- control socket is created
- SSH jail active and monitored

