1. finish configuring prometheus with custom metrics (such as discord alerts for main infra issues)
2. experiment with linux perf tuning, particularly in server environments
3. maybe mess around with resyncing clients on mc servers
4. look into the ins-and outs of CDNs and learn basic concepts
5. build failure analysis/postmortem pipelines (automate as much as possible? scripts that push an easily fillout-able template?)



#Achievable Stuff
- Finish troubleshooting prometheus for VM/CT monitoring
- Look into helping with other repos (get first pull request accepted)
- learn basic bash scripting (auto-filling template should be simple enough) 
- build local LLM and just kinda mess around with it
- fill out postmortem and find a format that makes sense, then standardize it in seperate repo 
- fix CEJ auto server launch script, (have it run in tmux, or uninstall tmux and change to screen)


#Further down-the-line projects
Use incoming ubiquiti switch to create enterprise level vlans and learn basic switching CLI
local physical seismic detector that can send emergency shutdown requests
auto shutdown script in power outage scenario (make sure all monitoring systems shutdown last)
Potential self hosted cloud service as experimental "S3" project
